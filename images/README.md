# Imágenes

Guarda aquí las imágenes optimizadas del sitio (`webp`, `avif`, `png` o `svg`).

La versión inicial utiliza principalmente recursos visuales construidos con CSS, por lo que solo requiere el favicon.
